{
    "version": 2,
    "buildCommand": "npm run build && npm run migrate:prod",
    "installCommand": "npm install",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/server.js"
      }
    ]
}
