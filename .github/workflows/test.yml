name: Run Tests

on:
  push:  # Acionar em todos os commits
    branches:
      - '*'  # Qualquer branch
  pull_request:  # Acionar em pull requests
    branches:
      - '*'  # Qualquer branch

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up
